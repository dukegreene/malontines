<li class="malontine-in-list">
  <img class="malone-photo" src=<%= asset_path(malontine.malone.image_url) %> alt="<%= malontine.malone.description %>">
  <h2>👆👆👆<%= malontine.love_note.message %>👆👆👆</h2>
  <h3>created by <%= malontine.creator.username %></h3>
</li>
