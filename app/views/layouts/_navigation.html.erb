<nav class="navbar navbar-default fixed-top bg-light border-bottom border-primary mb-1">
  <% if current_user %>
    <%= link_to current_user.nav_title, root_path %>
  <% else %>
    <%= link_to "😍 Malontines 😍", root_path %>
  <% end %>
  <form class="form-inline">
    <%= link_to "Post one", new_malontine_path, class: "btn btn-outline-secondary mr-1" %>
    <% if logged_in? %>
      <%= link_to "Log out", logout_path, method: :delete, class: "btn btn-outline-primary" %>
    <% else %>
      <%= link_to "Log in", login_path, class: "btn btn-outline-primary" %>
    <% end %>
  </form>
</nav>
